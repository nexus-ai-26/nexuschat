[project]
name = "wa-llm"
version = "1.4.10"
description = ""
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.18.5",
    "asyncpg>=0.31.0",
    "cachetools>=7.1.4",
    "fastapi>=0.139.0",
    "httpx>=0.28.1",
    "logfire[fastapi,httpx,sqlalchemy,system-metrics]>=4.37.0",
    "jinja2>=3.1.6",
    "pgvector>=0.5.0",
    "pydantic-ai>=1.73.0",
    "pydantic-settings>=2.13.1",
    "pydantic>=2.13.4",
    "sqlalchemy[asyncio]>=2.0.51",
    "sqlmodel>=0.0.39",
    "tenacity>=9.1.4",
    "uvicorn>=0.50.2",
    "voyageai>=0.4.1",
    "gowa>=0.1.0",
]

[dependency-groups]
dev = [
    "notebook>=7.6.0",
    "pandas>=2.3.3",
    "nbstripout>=0.9.1",
    "whatstk>=0.8.1",
    "ruff>=0.15.20",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "pytest-httpx>=0.36.2",
    "pyright>=1.1.411",
    "tqdm>=4.68.3",
    "poethepoet>=0.48.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.semantic_release]
version_toml = [
    "pyproject.toml:project.version"
]
branch = "main"
upload_to_pypi = false
upload_to_release = true
build_command = "pip install uv && uv build"

[tool.coverage.report]
exclude_also = [
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
    "pragma: no cover",
    "import *",
    # 1. Exclude an except clause of a specific form:
    "except ValueError:\\n\\s*assume\\(False\\)",
    # 2. Comments to turn coverage on and off:
    "no cover: start(?s:.)*?no cover: stop",
    # 3. A pragma comment that excludes an entire file:
    "\\A(?s:.*# pragma: exclude file.*)\\Z",
    "@pytest\\.fixture.*",
    "@pytest\\.mark.*",
]

omit = [
    "tests/*",
    "/tmp/*",
    "/private/*",
    "*/temporary/*"
]

[tool.pyright]
venvPath = "."
venv = ".venv"
typeCheckingMode = "basic"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
log_cli = true

[tool.logfire]
pydantic_plugin_record = "failure"

[tool.poe.tasks]
format = "ruff format"
lint = "ruff check"
typecheck = "pyright"
test = "pytest"

# Run format first, then lint/typecheck/test in parallel
check.sequence = ["format", { parallel = ["lint", "typecheck", "test"] }]
check.help = "Run all development checks (format, then parallel lint/typecheck/test)"

# For local development
# [tool.uv.sources]
# gowa = { path = "../sdk-python-gowa", editable = true }
